document.addEventListener("DOMContentLoaded", () => {

    const header = document.querySelector("main h1");

    header.addEventListener("click", () => {

        alert("Welcome to the Vanilla HTML + CSS + JS Assignment!");

    });

});